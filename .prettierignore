# O `three` e os add-ons de `jsm/` são cópias upstream: reformatá-los faz todo `diff` contra a
# origem virar ruído, e a próxima atualização vira conflito de estilo em vez de conflito de código.
vendor/

node_modules/
.cache/
assets/

# ☠️ **MARKDOWN fica de fora, e não é por medo de quebrar oráculo** — testado: com os docs
# formatados, `lei-tooling` e `lei-residentes` (os dois que LEEM doc) passam.
#
# O motivo é o diff FUTURO. Prettier alinha coluna de tabela por preenchimento, então trocar uma
# palavra numa célula repagina a coluna inteira — e estes documentos são tabela de ponta a ponta.
# Uma correção de uma linha passaria a chegar como quarenta. Medido no dia: 33 arquivos `.md`
# reescritos só pelo realinhamento.
*.md
